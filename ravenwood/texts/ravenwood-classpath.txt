framework-minus-apex.ravenwood.jar
kxml2-android.jar
json-prebuilt.jar
ext-ravenwood.jar

android.test.mock.ravenwood.jar
hoststubgen-helper-runtime.jar

# Note, when we include other services.* jars, we'll need to add
# platform_compat_config for that module too.
# See ravenwood-services.core-platform-compat-config.
services.core.ravenwood-jarjar.jar

# ICU
core-icu4j-for-host.ravenwood.jar
icu4j-icudata-jarjar.jar
icu4j-icutzdata-jarjar.jar

# DeviceConfig
framework-configinfrastructure.ravenwood.jar

# StatsD
framework-statsd.ravenwood.jar

# Graphics
framework-graphics.ravenwood.jar

# Provide runtime versions of ravenwood-utils
junit.jar
truth.jar
flag-junit.jar
ravenwood-framework.jar
ravenwood-junit-impl.jar
mockito-ravenwood-prebuilt.jar

{TEST_JARS}

# Below are the API stubs. We put them after the test jar in the classpath so
# that the test jar can include its own, non-stub version of mainline classes.

# System API stubs (== without proper method bodies) including
# @SystemApi(MODULE_LIBRARIES), from all updatable mainline modules.
# This doesn't contain the ART/libcore module and non-updatable
# modules, which we pick from all-updatable-modules-system-stubs.
# This allows mocking such APIs.
framework-updatable-stubs-module_libs_api.jar

# Fallback from all-updatable-modules-system-stubs. This contains
# System API stubs from all mainline modules, including ART/libcore
# non-updatable modules.
# This however does _not_ contain @SystemApi(MODULE_LIBRARIES) APIs.
# So if these modules contain such APIs, we still don't have them
# on Ravenwood.
all-modules-system-stubs.jar
